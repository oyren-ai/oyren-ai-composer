{
  "name": "oyren-agent",
  "displayName": "Oyren Agent",
  "description": "The sandbox agent behind VS Code's built-in Chat view — one participant for all seven providers.",
  "version": "0.1.0",
  "publisher": "oyren",
  "private": true,
  "license": "UNLICENSED",
  "engines": {
    "vscode": "^1.100.0"
  },
  "extensionKind": [
    "workspace"
  ],
  "main": "./extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "_comment.enabledApiProposals": [
    "isDefault + modes on the participant and isUserSelectable on the provider's models need these",
    "proposals at 1.109. They are matched against the launcher's --enable-proposed-api",
    "oyren.oyren-agent (see deploy/sandbox-host/start-editor.mjs). Do NOT move this list into",
    "product.json's extensionEnabledApiProposals: that key OVERRIDES an extension's own declaration",
    "rather than merging with it."
  ],
  "enabledApiProposals": [
    "defaultChatParticipant",
    "chatProvider"
  ],
  "contributes": {
    "chatParticipants": [
      {
        "id": "oyren.agent",
        "name": "oyren",
        "fullName": "Oyren",
        "description": "Ask the sandbox agent.",
        "isDefault": true,
        "isSticky": true,
        "modes": [
          "ask"
        ]
      }
    ],
    "languageModelChatProviders": [
      {
        "vendor": "oyren",
        "displayName": "Oyren"
      }
    ]
  }
}
