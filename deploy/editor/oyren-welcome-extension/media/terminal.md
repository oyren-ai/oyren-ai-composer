## Two screens, one terminal

The default terminal profile attaches to the same **tmux session** the Oyren app's Terminal pane
shows. Whatever the agent does there, you see live — in either place. Closing a tab only detaches;
the agent keeps running.

The profile dropdown (the `+` split button) also starts any other agent CLI, or a plain `bash` when
you just want a shell.
