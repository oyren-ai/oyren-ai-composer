# Flat node_modules (npm-style) so the builder stage's output copies cleanly into the runtime
# image across Docker stages — no dangling pnpm symlinks. Also lets the vendored runner's
# `import "yaml"` resolve from the shared top-level node_modules.
node-linker=hoisted
